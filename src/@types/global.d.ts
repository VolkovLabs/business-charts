/**
 * Extend window interface
 */
interface Window {
  /**
   * Gaode Maps
   */
  // eslint-disable-next-line @typescript-eslint/naming-convention
  AMap?: unknown;

  /**
   * Baidu Maps
   */
  // eslint-disable-next-line @typescript-eslint/naming-convention
  BMap?: unknown;
}
